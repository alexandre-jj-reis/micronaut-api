micronaut:
  application:
    name: alexandre

datasources:
  default:
    url: jdbc:mysql://localhost:3306/micronaut
    username: root
    password: password
    driverClassName: "com.mysql.cj.jdbc.Driver"
    dialect: MYSQL

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true